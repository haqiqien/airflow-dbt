{
  "name": "UGM Data Warehouse Devcontainer",
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "remoteEnv": {
    "AIRFLOW_HOME": "${workspaceFolder}airflow"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "Binnford.dbt-formatter",
        "innoverio.vscode-dbt-power-user",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg"
      ]
    }
  },
  "postCreateCommand": "mkdir -p ${workspaceFolder}airflow ${workspaceFolder}data && psql postgres://ugm_user:ugm_password@localhost:5432/procurement_dw -c 'CREATE SCHEMA IF NOT EXISTS raw; CREATE SCHEMA IF NOT EXISTS analytics;' && airflow db init && airflow users create --username admin --firstname UGM --lastname MKom --role Admin --email example@domain.com --password admin",
  "postStartCommand": "airflow webserver -D && airflow scheduler -D",
  "forwardPorts": [8080, 5432],
  "remoteUser": "vscode"
}
